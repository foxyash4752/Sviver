#ifndef CONFIG_H // Если не определено, то переходим к следующему
#define CONFIG_H // Импортируем нашу библиотеку CONFIG_H
#define PLATFORM_SVIVER // Импортируем нашу библиотеку PLATFORM_SVIVER
// #define PLATFORM_ARDUINO // Импортируем нашу библиотеку PLATFORM_ARDUINO
#endif // Заканчиваем наш #endif